/**
 * Describe money from number to string
 * 1. Split number in to 9 digits groups by divide to 1 billion and take the remainder.
 * 2. Add suffix "tỷ" responding to group's count
 * 3. Send each group to function read 9 digits:
 *      - split 
 * 
 * @param {money} money need to read 
 */
function readMoneyFromNumberToString(money){

}